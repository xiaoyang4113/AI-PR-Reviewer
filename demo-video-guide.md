# Demo 视频录制指南

> **时长建议：** 5-8 分钟  
> **录制工具：** Windows 自带 Xbox Game Bar（`Win + Alt + R`）或 [OBS Studio](https://obsproject.com/)  
> **上传平台：** bilibili（推荐，评委可在线播放）或百度网盘

---

## 一、录制前准备

| 准备项 | 操作 |
|--------|------|
| 启动后端 | `cd backend && ./mvnw spring-boot:run`（确认 8080 端口正常） |
| 启动前端 | `cd frontend && npm run dev`（确认 5173 端口正常） |
| 确认 AI Key | 检查 `application-dev.yml` 中 `api-key` 有效（非 Mock） |
| 准备测试 PR | 提前找一个自己的 GitHub 公开仓库 PR，复制好链接和编号 |
| 清理数据 | 可选：`DELETE FROM review_tasks WHERE pr_number = 测试编号`，避免重复提交拦截 |
| 关闭无关窗口 | 收起 IDE、微信、通知，保持桌面整洁 |

---

## 二、视频结构

### 第一段：开场介绍（约 40 秒）

**画面：** 展示浏览器打开的前端页面（`http://localhost:5173`）

**讲解词：**

> 大家好，我是 XXX。这是我参加七牛云 XEngineer 暑期实训营第二批次的作品——AI PR Review 助手。
>
> 它是一个基于 AI 的 GitHub Pull Request 代码评审工具。用户输入 GitHub PR 链接，系统自动获取代码变更，调用大模型进行智能分析，生成包括变更总结、风险识别和优化建议在内的结构化评审报告。
>
> 下面我来演示一下它的核心功能。

---

### 第二段：核心功能演示 · 提交评审（约 2 分钟）

**画面操作：**

1. 鼠标指向「GitHub 仓库链接」输入框，粘贴准备好的 PR 链接
2. 鼠标指向「PR 编号」输入框，输入数字
3. 点击「开始智能评审」按钮

**等待 AI 分析期间的讲解词（约 1 分钟）：**

> 点击提交之后，系统后端开始工作——先调用 GitHub REST API 获取这个 PR 的完整 diff 文本，然后把 diff 发送给 DeepSeek V4 Flash 大模型进行结构化分析。
>
> 我们设计了一套结构化的 System Prompt，从五个维度来审查代码：
> - 安全性——检测硬编码密钥、SQL 注入、XSS 等安全漏洞
> - 性能——排查内存泄露、死循环、N+1 查询等性能隐患
> - 健壮性——检查空指针、未处理异常、边界条件缺失
> - 规范性——识别命名不规范、魔法数字、重复代码
> - 最佳实践——评估设计模式使用和 SOLID 原则遵循情况
>
> 分析结果分为三级风险：CRITICAL 表示必须修复，WARNING 表示建议修复，INFO 表示建议优化。

---

### 第三段：展示评审结果（约 2 分钟）

**画面操作：**

1. 加载完成后，鼠标指向 AI 总结卡片

**讲解词：**

> 分析完成。最上方是 AI 生成的变更总结——一句话概括了这次 PR 的修改意图、涉及的文件范围和整体评价。

2. 鼠标指向评论列表，滚动浏览

**讲解词：**

> 下方是识别出的具体代码问题，按风险等级排序。每条评论包含：
> - 文件路径和行号
> - 风险等级标签——红色 CRITICAL、黄色 WARNING、蓝色 INFO
> - 详细的评审意见——说明为什么这是问题

3. 展开一条 CRITICAL 评论，指向红绿代码对比面板

**讲解词：**

> 点击展开可以看到代码对比——左侧红色区域是触发风险的原始代码，右侧绿色区域是 AI 给出的优化建议。比如这里检测到了硬编码密钥，AI 建议从环境变量读取 JWT 密钥，并给出了具体的修复代码。

4. 如果有评论数为 0 的结果，也可以提一下

**讲解词：**

> 如果 AI 认为代码质量良好，没有发现明显问题，也会给出明确提示，不会强行编造问题——这也是我们在 Prompt 中特意约束的"只报告实际存在的问题"。

---

### 第四段：历史记录功能（约 30 秒）

**画面操作：**

1. 向下滚动到「评审历史」区域

**讲解词：**

> 每次评审都会自动保存到数据库。下方是评审历史，显示每次评审的 PR 标题、状态标签和简要总结。

2. 点击一条历史记录

**讲解词：**

> 点击任意一条历史记录，可以重新查看完整的评审详情。新提交的评审也会自动追加到历史列表顶部。

3. 指向「加载更多」按钮

**讲解词：**

> 历史记录采用分页加载，点击"加载更多"可以浏览更早的评审结果。

---

### 第五段：技术架构讲解（约 1.5 分钟）

**画面：** 切换到 IDE，展示后端项目结构

**讲解词：**

> 接下来简单介绍一下技术架构。
>
> 后端采用 Java 17 + Spring Boot 3.2 构建，严格按照三层架构分层——Controller 层负责接口暴露和参数校验，Service 层做业务编排，Mapper 层用 MyBatis-Plus 做数据访问。
>
> 核心业务流程是 ReviewService 协调 GitHubService 和 AiAnalysisService 共同完成——先获取 PR Diff，再调用 AI 分析，最后将结果持久化到 MySQL 数据库。
>
> 数据库设计了两张表——review_tasks 存储任务状态和总结，review_comments 存储逐条评审意见，通过外键一对多关联。还在 repo_url 和 pr_number 上加了唯一索引，防止并发重复提交。
>
> 前端使用的是 Vue 3 + Vite + Tailwind CSS，采用暗色主题和响应式布局。提交评审后展示骨架屏加载动画，收到结果后展示带有风险等级颜色标签的评论卡片。
>
> 关于容错设计——当 AI API 不可用时，系统会自动降级为 Mock 演示数据，并在前端顶部显示醒目的琥珀色警告横幅，告诉用户当前是演示数据以及原因。

---

### 第六段：设计思路说明（约 1 分钟）

**画面：** 切换到 README 文档的「设计思路说明」部分

**讲解词：**

> 关于题目要求的三个设计思路：
>
> 第一，模型选择。我们选用 DeepSeek V4 Flash，它拥有 100 万 token 的上下文窗口，响应速度快，成本低，对代码审查场景完全够用。通过配置即可无痛切换为 Pro 模型或其他厂商的模型。
>
> 第二，上下文获取方式。我们通过 GitHub API 的 diff 专用 Accept 头获取完整 diff 文本，包含文件路径和行号信息。对于超大 PR 做了 8000 字符的截断保护，防止超出模型 token 限制。
>
> 第三，未来扩展方向。我们预留了策略模式接口，可以方便地扩展支持 GitLab、Gitee 等多平台。当前是同步模式，后续可通过 Spring 的 @Async 异步方案结合 WebSocket 实现实时进度推送。还可以集成到 CI/CD 流水线中，作为 GitHub Actions 的一环自动触发评审。

---

### 第七段：总结（约 20 秒）

**画面：** 回到前端主页面

**讲解词：**

> 以上就是我的 AI PR Review 助手作品。它在 72 小时内从零开始，完成了需求分析、架构设计、前后端开发、测试优化和文档编写全流程。希望能帮助开发者提升 Code Review 的效率和质量。
>
> 感谢观看，欢迎评委老师提出宝贵意见。谢谢！

---

## 三、录制后处理

### 1. 上传到 bilibili

1. 打开 [bilibili.com](https://www.bilibili.com/) → 登录 → 右上角「投稿」
2. 上传视频文件 → 标题填：`AI PR Review 助手 - 七牛云 XEngineer 实训营作品演示`
3. 分区选「科技 → 软件应用」
4. 简介里粘贴项目简介和 GitHub 仓库链接
5. 标签填：`七牛云`、`XEngineer`、`AI代码审查`、`Spring Boot`、`DeepSeek`
6. 发布后把视频链接复制下来

### 2. 更新 README

在 `README.md` 的 Demo 视频段落（约第 380 行）中填入链接：

```markdown
## Demo 视频

> 视频链接：[https://www.bilibili.com/video/BVxxxxxxxxxx](替换为你的链接)
```

---

## 四、注意事项

- 声音清晰、语速适中，不要太快。可以对着稿子念，自然即可
- 操作鼠标时放慢动作，让观众看清楚点击了哪里
- 提前过一遍流程，确保不会卡壳或报错
- 测试用的 PR 最好是自己仓库的，不要用别人的热门仓库（避免隐私争议）
- 如果某一步出了小问题不要紧张，继续讲，评委看重的是整体效果
- 视频开头和结尾不要留太长的空白
